# Yieldly Client

React + Vite frontend for the Yieldly portfolio tracker.

Dev server runs on `http://localhost:2080` and proxies `/api/*` to the Express server on port `2085`.

See the root [README](../README.md) for setup and usage.
