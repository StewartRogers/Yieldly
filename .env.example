# Optional: AlphaVantage API key (reserved for future market-data integration)
ALPHA_KEY=

# Session secret for authentication (required for production — sessions won't
# persist across server restarts without this)
SESSION_SECRET=

# Set to "1" if behind a reverse proxy with HTTPS (enables secure cookies)
TRUST_PROXY=
